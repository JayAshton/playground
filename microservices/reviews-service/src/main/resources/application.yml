spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/app_db
    username: pguser
    password: S3cret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect